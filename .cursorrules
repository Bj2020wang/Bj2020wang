# 项目协作规则（持续生效）

## 功能开发：先思路、再动手（与用户约定 · 强制）

当用户提出**功能类、行为类或范围明确的改动**（非单纯信息问答）时，AI **必须先**与用户对齐实现方案，**再**编写或修改代码。思路说明建议包含：

1. **对需求的理解**（一两句话复述，避免偏题）。  
2. **实现思路**（步骤、数据流、用户可见行为）。  
3. **计划改动范围**（主要文件/模块、云函数 action、是否新建集合等）。  
4. **易分歧点与取舍**（边界情况、可选方案、默认假设）。  
5. **依赖与风险**（需部署云函数、改库表规则、兼容性等）。

**待用户明确确认**（例如「可以按这个做」「同意」或逐条回复修改意见）后，再进入开发与提交。若用户在同轮已使用第 9 条中的**加速关键词**（如「直接改」「快速实现」「小修复 / hotfix」等），则按该条例外执行。  
**极小改动**（如明显错字、单处笔误、一行级且无行为歧义的修复）：可先**一句话说明**再改，无需长篇方案。

---

1. EXE 版本号必须与 Git 标签版本号一致。
   - 例如：`tauri.conf.json` 的 `version` 为 `0.2.6`，则 Git 标签必须是 `v0.2.6`。

2. 每次发布前必须走发布脚本：
   - 命令：`powershell -ExecutionPolicy Bypass -File .\scripts\release.ps1`
   - 作用：校验工作区干净、校验标签一致、打包 EXE、归档到 `release/v版本号/`。

2b. Windows 本地手动执行 `tauri build` 时，须与 `scripts\release.ps1` 使用同一套 Cargo 输出约定（写入 `PROJECT_GUIDE.md` §2.7）：
   - `CARGO_TARGET_DIR` = `C:\tbuild\app`（可先 `New-Item -ItemType Directory -Force C:\tbuild\app`）
   - `CARGO_BUILD_JOBS` = `1`
   - `RUSTFLAGS` = `-C debuginfo=0`
   - 安装包路径：`C:\tbuild\app\release\bundle\nsis\Todo Calendar_<version>_x64-setup.exe`

3. 发布包归档规则：
   - 安装包固定放在 `release/vX.Y.Z/` 目录下，便于回溯和分发。

4. CloudBase 接入特殊性（已踩坑记录）：
   - 鉴权是双层：云函数 `invoke` 权限与 HTTP 路由鉴权是两套配置，只放开一层仍可能报 `MISSING_CREDENTIALS`。
   - HTTP 请求体常在 `event.body`（字符串），不能直接读取 `event.action`，需先做 JSON 解析。
   - `@cloudbase/node-sdk` 的 `add/update` 直接传字段对象，不使用 `data: {...}` 包裹。
   - 调试与生产必须分离：生产云函数以仓库 `cloudfunctions/newworld/index.js` 为准（无 `debugCode`、无 `debug-codes`）；控制台若曾保留调试版，发布前需对齐该文件。
   - 当前邮箱验证码发码方案固定为 CloudBase 内置邮件服务（`/auth/v1/verification`），不是 21cn SMTP 直发；若要切回自定义 SMTP，必须先评审并同步更新文档与规则。
   - PowerShell 调用优先使用 `Invoke-RestMethod` 或 `curl.exe`，避免 `curl` 别名与 `>>` 粘贴问题。
   - 云函数改代码优先走 `函数详情 -> 函数代码 -> index.js`；新增依赖后优先使用“保存并安装依赖”，再发布并回归测试。
   - `ACTION_FORBIDDEN` 常见根因在 HTTP 路由身份认证层；开发联调阶段可先关闭路由身份认证以稳定推进，生产前再收紧。

5. 多客户端同步规则（新增）：
   - 冲突判定优先依据 `version`，`updatedAt` 仅用于展示，不作为唯一同步依据。
   - 禁止默认整包覆盖；优先采用分条合并（todo/event）和按 key 合并（note）。
   - 删除操作必须记录墓碑时间（tombstone），防止旧端数据复活。
   - **笔记清空也必须记录墓碑**：`notesByDate` 删除某日期内容时，同时更新 `noteMetaByDate` 与 `noteTombstonesByDate`；合并时若墓碑时间 >= 文本更新时间，必须保持为空，禁止旧文本回流复活。
   - 自动同步必须分流决策：仅本地改动自动推、仅云端改动自动拉、双方改动先拉后推。
   - 首次登录未完成一次成功拉取前，禁止开启自动同步。
   - 自动处理结果必须可见：展示运行状态（未登录/同步中/空闲）、合并提示、上次拉取/推送时间。
   - **App 级防抖自动 push**（2026-04-30）：`App.tsx` 在已登录且 `localStorage` 已标记完成首次拉取（`todo-calendar-first-pull-done`）后，对 Todo/事件/笔记等本地变更约 1.2s 内自动调用 `pushSnapshot`，与账号弹窗内手动推送同接口。
   - **退出登录**（2026-05-01）：`AccountLoginModal` 在推送与 `logout()` 前让用户二选一——**清空本机日历**（`onAfterLogout` → `App` 删 `todo-calendar-local-v1` 与协作/同步相关键并重置 UI）或 **仅退出保留本地**。

6. CloudBase 混合同步架构（2026-04-30 定型）：
   - **账号与快照主路径**仍为云函数 HTTP `newworld`（发码、验码、`pull`、`push`）；`user_snapshots` 可增加 **`dbAuthUid`** 字段，规则示例：`read`/`write` 均为 `doc.dbAuthUid == auth.uid`；验码接口可返回 **`customLoginTicket`**（云函数配置 `TCB_CUSTOM_LOGIN_PRIVATE_KEY` / `TCB_CUSTOM_LOGIN_PRIVATE_KEY_ID` 时）。
   - **全应用共享一份登录态**：仅在 `main.tsx` 使用 `AccountAuthProvider`，业务与弹窗统一使用 **`useSharedAccountAuth()`**；禁止在 `App` 与 `AccountLoginModal`（或其它同级组件）各自调用 **`useAccountAuth()`** 导致 token / `baseVersion` 不一致。
   - **文档库 `watch`** 在当前环境常报 `INIT_WATCH_FAIL` / `SYS_ERR`；**个人云**下产品侧以 **`watch` 与云函数 `pull` 定时轮询（约 15s）并行** 为下行主路径（`watch` 成功时仅加速）；协作工作区仍不以个人云 `watch`/`pull` 替代 **`team-pull`**。`watch` 仍保留作环境修复后的优化路径。
   - **直连 `get` 轮询**在客户端规则/登录态未对齐时易返回空，下行兜底已改为 **HTTP `pull`**，与手动「拉取云端」一致。
   - 调试前缀 **`[sync-debug]`**：修改 watch / poll / 自动推送相关逻辑时，保留或更新对应日志，便于用户控制台验收。

7. 双人协作与双云隔离（2026-05-01 / v0.2.0）：
   - **个人云** `user_snapshots` 与 **协作云** `team_snapshots` **数据隔离**；工作区切换见 `App.tsx` 与 `config.ts`（`WORKSPACE_MODE_KEY`、`ACTIVE_TEAM_ID_KEY` 等）。
   - 云函数 `newworld` 协作 action：`team-create`、`team-join`、`team-leave`、`team-get`、`team-pull`、`team-push`、`team-set-peer-read-only`；字段 **`peerReadOnly`** 为 true 时仅 **`ownerEmail`** 可 `team-push`。
   - 文档 **`snapshot` 为 `null`** 时，`push` / `team-push` 须用 **`db.command.set`（`_.set`）** 写入整对象，避免 Mongo 报错 `Cannot create field ... in element {snapshot: null}`（实现见 `snapshotForStore`）。

8. CloudBase 环境配置约定（2026-04-30 新增）：
   - 客户端必须显式配置 `VITE_CLOUDBASE_ENV_ID`，禁止在代码中保留可用默认环境 ID。
   - 未配置环境 ID 时应在初始化阶段给出明确报错提示，避免静默连到错误环境。
   - 仓库需维护 `.env.example` 作为配置模板，示例值仅用于说明，不得写入真实密钥。

9. AI 协作开发流程（七阶段 · 适配本项目）：
   说明：本项目为 **Tauri + Vite + React + TypeScript 桌面/Web 前端**，后端以 **腾讯云 CloudBase 云函数 HTTP（`cloudfunctions/newworld`）** 与 **文档库/集合** 为主，**不是**典型自建 Node.js Express 分层仓库。下列流程在「后端」处一律指云函数、HTTP 访问服务、数据库规则与控制台配置；与文首 **《功能开发：先思路、再动手》**、第 1～8 条及「最小改动范围」同时生效。除非用户明确说 **「快速实现」**、**「跳过审查」**、**「小修复 / hotfix」**、**「原型验证」**、**「直接改」**，否则按阶段推进；用户说了关键词时按该关键词对应的例外处理（见 9.8）。

   9.1 阶段 0 — 开工回顾  
   - 当用户说「继续开发」「开工」「开始今天的工作」或等价表述时：优先阅读 `PROJECT_GUIDE.md` 与近期 Git 提交/待办；若无独立 `docs/features.md`，**不强制创建**，以 `PROJECT_GUIDE.md` 与对话为准。  
   - 简要列出：已实现能力、待办/风险；请用户确认**本次唯一或少量优先目标**后再进入 9.2。

   9.2 阶段 1 — 梳理业务逻辑（须用户确认后再改代码）  
   必须覆盖三部分（不能只写正常路径）：  
   - **正常流程（Happy Path）**：步骤化描述。  
   - **异常与边界**：空输入、未登录、token 失效、409 版本冲突、云函数/HTTP 403、权限规则拒绝、网络失败、多端同时改等。  
   - **模块与依赖拆分**：  
     - **前端**：页面/组件（如 `App.tsx`、`AccountLoginModal.tsx`、`features/account/*`）、状态（Context / hooks）、API 封装（`authApi.ts` / `config.ts`）。  
     - **云端**：`cloudfunctions/newworld/index.js` 中 action 分支、集合读写、`user_snapshots` / `team_snapshots` / `user_tokens` 等；**数据库安全规则**、**索引**（如 `email` / `dbAuthUid` / `teamId`）、**环境变量**（云函数侧 `TCB_*`、前端 `VITE_*`）。  
   - **对外依赖清单**：是否新建/修改集合、是否改 HTTP 路由鉴权、是否依赖自定义登录等。

   9.3 阶段 2 — 规划改动点（须用户确认后再动代码）  
   - 输出**改动清单表**（路径 | 新增/修改 | 摘要 | 是否新依赖）。  
   - **前端**常见：`src/**/*.tsx`、`src/features/**`、`src/types`、样式。  
   - **云端**常见：`cloudfunctions/newworld/index.js`、控制台发布说明。  
   - **自检**：类型与常量、`.env.example`、安全规则/索引是否在说明中体现；**禁止**在规则或示例中提交真实密钥。

   9.4 阶段 3 — 实现代码  
   - **前端**：函数组件 + Hooks；Props 用 interface；异步用 try/catch；与现有项目风格一致；**最小范围改动**。  
   - **云端**：与现有 `newworld` 风格一致；`event.body` JSON 解析；错误与 HTTP 状态与前端 `authApi` 约定一致。  
   - **禁止**用「// … 其余不变」代替完整可应用片段；改动大时给出完整文件或明确可复制的整块替换内容。

   9.5 阶段 4 — 功能审查（测试视角）  
   - 对照 9.2 的正常/异常/边界与 9.3 清单，逐条核对是否实现。  
   - 输出问题表（序号 | 类型 | 描述 | 对应需求点 | 阻塞/一般/建议）。**只评功能与行为**，不评代码风格。

   9.6 阶段 5 — 质量审查（代码视角）  
   - 前端：无滥用 `any`、loading/error 状态、敏感信息不进日志、与 Tauri/CloudBase 约束一致。  
   - 云端：鉴权与频控、不在响应中泄露内部栈、集合权限最小化。  
   - 输出改进表（序号 | 描述 | 位置 | 必须/建议 | 改进建议）。

   9.7 阶段 6 — 用户侧验证  
   - 由用户在本地/EXE 验证；反馈问题后：根因 → 方案 → 修复 → 可再触发 9.5/9.6（按需）。

   9.8 阶段 7 — 知识沉淀  
   - 功能稳定后：更新 `PROJECT_GUIDE.md` 相关章节；重大约定同步本条 `.cursorrules`。  
   - 可采用「功能名 + 日期」小节：业务规则、关键决策、边界情况、已知技术债、涉及云函数 action / 集合 / 环境变量表。

   9.9 例外关键词（加速模式）  
   - **「快速实现」**：可跳过 9.5、9.6，9.1～9.3 仍须执行（或用户口头等价确认范围）。  
   - **「小修复」/「hotfix」**：可压缩 9.1～9.2，但仍须说明改动文件与风险；**必须**做 9.5 级快速核对。  
   - **「原型验证」**：可跳过 9.5、9.6、9.7 文档，代码可用 TODO 标注后续偿还项。  
   - **「直接改」**：仅输出最小补丁；仍须遵守用户「修改前同意」规则——若用户未在该轮明确同意，须先征得同意。

10. 协作笔记分权规则（作用范围：`src/**/*.{ts,tsx}`）：
   - 当协作权限为 `peerReadAllWriteOwn`（对方可读全部、仅写自己）时，**笔记必须与任务一致按人分权**：队友只能修改自己名下笔记，不能修改对方笔记。
   - 进入协作快照组装（`buildWriteOwnTeamSnapshot`）时，Todo/Event/Note 三类数据都要按“当前写入者归属”处理，禁止只对任务分权、遗漏笔记。
   - 侧栏笔记区若为队友笔记，第一行必须显示点赞标识（`ThumbsUp`）和“队友笔记（只读）”提示，并禁用编辑与保存按钮，避免误改。
   - 新增或调整协作相关功能时，必须同时回归这三种权限：`bothPush`、`peerReadOnly`、`peerReadAllWriteOwn`，确保笔记与任务行为一致。

11. 代码质量验证节奏（固定执行）：
   - 每次功能开发都执行四次验证：
     1) **改代码前（3~5 分钟）**：先做影响面与风险点检查，确认回归范围。
     2) **改完立即（开发者自测）**：至少运行类型构建（`npx tsc -b`）并走关键路径手测。
     3) **提交前（严格审查）**：按严重级别审查逻辑一致性、数据边界、删除语义、异常路径和规则符合性。
     4) **发版前（release gate）**：校验版本号、文档、Git 标签、安装包一致性与可安装性。
   - 最低门槛：**功能完成后必须先通过第 2+第 3 次验证再允许提交**；若当天发版，收工前必须完成第 4 次验证。

12. Git 双远程推送（GitHub + Gitee · 强制）：
   - **约定**：每次向远端推送时，须**同时**推送到 GitHub 与 Gitee，避免只更新一侧。
   - **本仓库已配置**：`origin` 具有 **双 `pushurl`**（先 Gitee、后 GitHub，地址见下）。日常 **一条命令即可推两边**：
     - 分支：`git push origin <branch>`（`<branch>` 如 `master`）
     - 标签：`git push origin --tags`
     - 无需再跟一条 `git push gitee`（除非本地未配置双 `pushurl` 或需单独修某一远端）。
   - **Gitee 仓库**：`https://gitee.com/wang-jianjun2025/todolist.git`；**单独远程名** `gitee` 仍保留，便于拉取或排查；与 `origin` 双 push 并存。
   - **新克隆 / 未配置双 pushurl 时**：先 `git remote add gitee https://gitee.com/wang-jianjun2025/todolist.git`，再对本机 `origin` 执行：
     - `git remote set-url --add --push origin https://gitee.com/wang-jianjun2025/todolist.git`
     - `git config --add remote.origin.pushurl https://github.com/Bj2020wang/Bj2020wang.git`（若上一步把 GitHub 从 push 列表挤掉，用此补回；以 `git remote -v` 确认 `origin` 下有两条 push）
     - 或退而求其次：依次执行 `git push origin <branch>` 与 `git push gitee <branch>`（及各自的 `--tags`）。
   - **AI 协助推送时**：优先使用 **`git push origin …`**（覆盖双远端）；若用户环境只有单 push，则分别 `origin` + `gitee`。若某一远端因网络失败，须明确告知哪一侧成功、哪一侧待重试。

13. **UI / 布局改动默认仅移动端（除非特别声明）**  
   - 用户未写明「桌面」「桌面端」「PC」「两边」「全端」等时，**一律视为只改移动端**（PWA / 窄屏 / `isMobileLayout` / `layout === 'mobile'` 等分支）。  
   - **桌面端布局与交互保持不动**；若组件与桌面共用（如 `DayView`、`CalendarGrid`），须拆成按布局分支渲染或新增仅移动端使用的封装，避免默认改掉桌面。  
   - 实现或复述方案时，可主动写一句「本次仅移动端」，减少误解。

14. **Todo「无时刻」默认归属日（导航日 · 产品约定）**  
   - 用户新建任务时若**未选择日期时间**（无定时、无日程时刻），任务的归属日与关联「无明确时间」日程的日期必须基于 **`currentDate`（当前日历导航日）**，例如 `toDateKey(currentDate)`；**禁止**用系统真实「今天」`new Date()` / `todayKey` 作为该路径的默认日期。  
   - 用户已自选日期或时刻的路径不受影响；若另有「日期无效时的回退」等行为，须在方案里单独说明是用导航日还是今日，避免静默混用。
